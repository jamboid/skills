# CSS token target architecture: [Project Name]

- Project:
- Date: [YYYY-MM-DD]
- Formalized by: Jamie Boyd

## Inputs

<!-- The architect formalizes; it doesn't invent. Record what it was fed, so a
     thin target can be explained by a thin conventions file rather than mistaken
     for "nothing much is wrong". -->

- `audit.json` from (date / CSS tree):
- Dispositions recorded at time of formalizing:
- House rules promoted:

## Intended architecture

<!-- Where this token system is meant to end up. Tiers (primitive → semantic →
     component)? Naming grammar? Theming approach? The target.json can only carry
     what was curated — this is where the intent that hasn't been curated yet
     lives, and it's the seed for the later formalization passes. -->

## Decisions to revisit

<!-- Stale decisions (the audit no longer raises them — gone, or the audit moved),
     dispositions that now look wrong, findings never triaged. Specific beats
     general. -->

# CSS token audit: [Project Name]

- Project:
- Date: [YYYY-MM-DD]
- Audited by: Jamie Boyd

## Which CSS was audited

<!-- The COMPILED / served CSS is the source of truth — the only place var()
     resolves. Record the exact tree you pointed the audit at, and note any
     build step (Sass, PostCSS, CSS @function, utopia.clamp, …). If parseErrors
     came back > 0, you were probably parsing authored source — re-point at the
     compiled output. -->

- CSS tree audited (absolute path):
- Build step / preprocessor (if any):
- Parse errors reported:

## Intended architecture

<!-- What scheme is this codebase MEANT to have? Tiers (primitive → semantic →
     component)? Naming grammar? Theming approach? Context the graph can't show —
     e.g. tokens that are a deliberate public API and so look "dead" but aren't. -->

## Observations

<!-- Anything the reconstruction got right/wrong, findings to confirm or dismiss,
     dead tokens that are actually intentional, load-bearing tokens that surprised
     you. Specific beats general. -->
